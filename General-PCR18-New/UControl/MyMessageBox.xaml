<Window x:Class="General_PCR18.UControl.MyMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:General_PCR18.UControl"
        xmlns:lang="clr-namespace:General_PCR18.Lang"
        mc:Ignorable="d"
        Title="{Binding Res.msgbox_title, Source={x:Static lang:RService.Current}}" Height="206" Width="420" ShowInTaskbar="False"
WindowStartupLocation="CenterScreen" BorderThickness="1,0,1,1"  Closed="Window_Closed" ResizeMode="NoResize">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <!--显示图片和文本-->
        <StackPanel Grid.Row="0" VerticalAlignment="Center" Orientation="Horizontal">
            <Image Source="{Binding ImagePath}" Width="62" Height="62" Margin="40,20,20,20"/>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" TextWrapping="WrapWithOverflow" 
                   Width="260" TextAlignment="Left"
                   Text="{Binding MessageBoxText}" FontSize="14"/>
        </StackPanel>
        <!--Button Margin(坐上右下)-->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{Binding Res.btn_ok, Source={x:Static lang:RService.Current}}" x:Name="OkButton" Width="80" Height="25" Click="OkButton_Click" Margin="10,0,15,0" IsDefault="True"
                Visibility="{Binding OkButtonVisibility,Mode=OneWay}"/>
            <Button Content="{Binding Res.btn_yes, Source={x:Static lang:RService.Current}}" x:Name="YesButton" Width="80" Height="25" Click="YesButton_Click" Margin="10,0,15,0" 
                Visibility="{Binding YesButtonVisibility,Mode=OneWay}"/>
            <Button Content="{Binding Res.btn_no, Source={x:Static lang:RService.Current}}" x:Name="NoButton" Width="80" Height="25" Click="NoButton_Click" Margin="10,0,15,0"
                Visibility="{Binding NoButtonVisibility,Mode=OneWay}"/>
            <Button Content="{Binding Res.btn_cancel, Source={x:Static lang:RService.Current}}" x:Name="CancelButton" Width="80" Height="25" Click="CancelButton_Click" Margin="10,0,15,0"
                Visibility="{Binding CancelButtonVisibility}"/>
        </StackPanel>
    </Grid>
</Window>
